{ config, pkgs, lib, ... }:
let 
in
{
  ############################################################
  # FROM NETWORKING.NIX
  ############################################################
  # networking.networkmanager = {
  #   enable = true;
  #   dhcp = "dhcpcd";
  # };
  # networking.firewall.package
}