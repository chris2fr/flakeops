{ config, pkgs, lib, ... }:
let 
in
{
  ############################################################
  # FROM USERS.NIX
  ############################################################
  # users.users.fossil = rec {
  #     isNormalUser = true;
  #     openssh.authorizedKeys.keys =  mannchriRsaPublic ;
  # };
}